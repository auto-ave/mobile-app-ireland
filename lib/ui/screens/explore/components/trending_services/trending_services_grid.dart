import 'package:flutter/material.dart';

class TrendingServicesGrid extends StatelessWidget {
  const TrendingServicesGrid({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
